import type { NextConfig } from "next";

/**
 * Security headers applied to every response (OWASP secure-header set).
 * CSP is deliberately absent: Next.js App Router requires inline bootstrap
 * scripts and Tailwind inline styles, so a nonce-less CSP adds no real
 * protection here — see PAD §6 for the full rationale.
 */
const securityHeaders = [
  { key: "X-Frame-Options", value: "DENY" },
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=()" },
  { key: "Strict-Transport-Security", value: "max-age=31536000; includeSubDomains" },
];

const nextConfig: NextConfig = {
  output: "standalone",
  reactStrictMode: true,
  async headers() {
    return [
      {
        source: "/:path*",
        headers: securityHeaders,
      },
    ];
  },
};

export default nextConfig;
